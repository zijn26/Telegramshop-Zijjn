# Exact pins for reproducible installs (no ~= minor/patch drift).
# Bump a version deliberately, then run the test suite.

# Core
python-dotenv==1.0.1
aiogram==3.22.0
SQLAlchemy==2.0.43
alembic==1.16.5
aiohttp==3.12.15
redis==6.4.0

# Database drivers
asyncpg==0.30.0
aiosqlite==0.20.0

# Admin panel
sqladmin==0.16.1
WTForms==3.1.2
starlette==1.3.1
uvicorn==0.49.0
itsdangerous==2.2.0

# Validation & Security
pydantic==2.5.3
cryptography==44.0.3

# Utils
python-dateutil==2.8.2

# Testing
pytest==8.4.2
pytest-asyncio==0.24.0
pytest-cov==4.1.0
pytest-mock==3.12.0
